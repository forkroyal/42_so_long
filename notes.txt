	MAPSTUFF.c notes darunter beachten
	MAPSTUFF.c notes darunter beachten
	MAPSTUFF.c notes darunter beachten

	function machen fillstruct darin make_map
	muss ich dann die struct als pointer machen?
	oder zumindest als pointer übergeben

Map:

	DONE! function to check if there are other signs then the ones allowed
	check for valid path > floodfill
	after valid path > check with strchr if collectables are left
	DONE !function to find P (startingpos) or E

Winwdow:

Main:
	check check_input
	open
	fill struct
	check validity with against struct
	maybe create a stuct called valid to safe valid values and compare against them



